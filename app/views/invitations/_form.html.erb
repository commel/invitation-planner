<%= form_for [@event, @invitation] do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% @invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control", placeholder: "Name des Gasts" %>
  </div>
  <div class="form-group">
    <%= f.label :invited %><br>
    <%= f.check_box :invited, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :accepted %><br>
    <%= f.check_box :accepted, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :payed %><br>
    <%= f.check_box :payed, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.button "Speichern", class: "btn btn-lg btn-primary btn-block" %>
  </div>
<% end %>
